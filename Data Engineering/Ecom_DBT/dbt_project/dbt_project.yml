name: 'ecom_analytics'
version: '1.0.0'
config-version: 2
profile: 'ecom_analytics'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
analysis-paths: ["analyses"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  ecom_analytics:
    staging:
      +materialized: view
    marts:
      +materialized: table
